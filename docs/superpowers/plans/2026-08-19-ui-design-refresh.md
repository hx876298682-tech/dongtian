# UI 设计稿适配实施计划

1. 替换 `apps/web/src/styles.css` 的全局 token、壳层布局、按钮、面板和响应式规则，保留现有语义 class。
2. 微调 `apps/web/src/app.tsx` 的壳层标记，使品牌、导航、挂机条、角色栏和日志更贴近设计稿的分区关系。
3. 为页面级常用 class 增加统一的铜线、青绿工作区、标题层级和列表密度规则，覆盖 12 张设计稿对应页面。
4. 运行类型检查、构建和页面级浏览器检查，按截图结果修正溢出、对比度和窄屏布局。
5. 更新 `docs/工作进度记录.md`，记录本次设计稿适配的范围、验证命令和遗留风险。

