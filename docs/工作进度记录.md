# 《洞天》工作进度记录

本文档持续记录项目开发进度、工作详情、验证结果、已知问题和后续计划，供恢复上下文与整理周报使用。

### 2026-08-17 浏览器通知设置接入

- 全局游戏反馈会读取 `dongtian.game-settings.v1`，仅在玩家已开启桌面通知、页面处于后台且浏览器已授权时发送系统通知。
- 设置页开启桌面通知时会请求浏览器权限；拒绝授权不会保存为开启状态，关闭通知不触发额外权限请求。
- 验证通过：Web TypeScript 检查、`apps/web/src/app.test.tsx`（3/3）、全仓 ESLint、`git diff --check`。
- 本机仍使用 Node `22.22.3`，仓库声明 Node `24.x`；当前命令仅产生 engine warning。

## 当前总览

### 2026-08-19 UI 设计稿浏览器复验

- 按用户要求重试真实页面验证。先确认前端停留初始化态的原因是 API 进程未监听 `3000`，而不是前端错误。
- 使用 `.env.example` 并补齐本地可选服务占位变量后，API 成功启动；浏览器匿名会话成功，`/dashboard/cave` 可以进入真实洞府内容。
- 桌面 `1440×900`：文档宽度与视口一致（`scrollWidth=1440`），无横向溢出；移动 `390×844`：页面出现移动端主导航，布局无横向溢出。
- 抽查 `/cultivation`、`/craft`、`/expedition`、`/settings`，均正常渲染统一壳层；浏览器 error/warn 日志为空。
- 验证完成后已停止本轮临时前端/API 进程，未修改业务代码。

### 2026-08-19 UI 设计稿适配

- 读取并核对 `ui 设计图/` 下 12 张 `1586×992` 设计稿，确认对应洞府、修炼、突破、百艺、采药、挖矿、炼丹、炼器、历练、角色、背包、设置页面。
- 用户确认按整套 UI 改造：以洞府设计稿为全局壳层基准，统一左导航、顶部挂机 / 资源栏、中央工作区、右角色栏和移动端底部导航；保留现有 API、队列、错误态、空态和权限态。
- 新增设计说明：`docs/superpowers/specs/2026-08-19-ui-design-refresh.md`；新增实施计划：`docs/superpowers/plans/2026-08-19-ui-design-refresh.md`，已提交 `52b47bd docs: add ui design refresh spec and plan`。
- `apps/web/src/styles.css` 增加统一的墨黑、青绿、旧纸白、金铜 token 和覆盖规则：移除大圆角玻璃卡片与强渐变，改为细铜线、低阴影、高密度工作区；桌面三栏、窄屏右栏 overlay、移动底部导航均保留。
- 验证通过：`pnpm --filter @dongtian/web typecheck`、`pnpm --filter @dongtian/web build`；构建仅有 Node `22.22.3` 相对仓库 `24.x` 的 engine warning 和既有 bundle 大小提示。
- 浏览器已启动 `http://localhost:5173/dashboard/cave`，前端无 console error/warn；由于本地 API / 匿名会话未启动成功，页面停留在“正在初始化匿名会话”，本轮无法取得已登录页面截图，未将视觉截图验收标记为通过。

### 2026-08-17 背包与装备 parity 子任务

- 装备页新增真实实例详情弹窗：显示中文装备名、绑定状态、强化、当前 / 比较预设上下文；关闭弹窗保留 `instance_id` 选中状态。
- “进入淬炼”复用现有淬炼目标状态、定位淬炼台并给出提示；`+6` 以上入口明确禁用，未新增交易或伪造 API。
- `GameDialog` 增加通用 `primaryDisabled`；新增装备详情展示测试，目标测试 3/3 通过，ESLint 与 `git diff --check` 通过。
- 发现计划中的 `apps/web/src/features/inventory/inventory-page.tsx` 在当前仓库不存在，实际背包页位于 `content-page.tsx`；本子任务未越界修改该文件，待主代理决定路径调整。

- 记录日期：2026-08-17
- 当前分支：`main`
- 当前阶段：M5“筑基 Endgame”主体完成，真实验收已通过
- 版本性质：可运行的开发纵向切片，不是 Alpha 发布候选
- 规格基线：`docs/开发前文档包/10_开发拆解里程碑与垂直切片_V1.0.md`、`docs/开发前文档包/14_Luna代码开发实施规格_V1.0.md`

## 里程碑判断

| 里程碑          | 判断                 | 证据与缺口                                                                                                                                                                 |
| --------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M0 工程地基     | 主体完成             | 已有 Monorepo、统一工具链、环境校验、13 组数据库迁移、配置发布包、OpenAPI、日志和本地基础设施；仍缺公开仓库 CI 与部署环境出口验证                                          |
| M1 时间内核     | 主体完成             | 已有认证会话、角色、资产 / 账本、幂等 / Outbox、确定性规则、结算服务及 Worker 分段续算                                                                                     |
| M2 闭关切片     | 主体完成             | 已有队列、输入预留、采药 / 炼丹、Buff、首页摘要、百艺和背包流程                                                                                                            |
| M3 秘境切片     | 主体完成             | 已有装备、战斗、秘境机会、青蛇洞状态机、恢复机制和 Web 历练流程；需继续以数据库 E2E 保持回归                                                                               |
| M4 生产成长     | 主体完成             | 已有挖矿 / 炼器内容、工具分配、装备整理、资源可达性校验和相关测试                                                                                                          |
| M5 筑基 Endgame | 主体完成，真实验收通过 | 筑基条件、资源预留、断线恢复、试炼选择、原子升级、筑基 Web 闭环、三槽库存条件队列和 3 条毕业 E2E 规格均已实现；真实 PostgreSQL 18.6 + Chrome 三条 E2E 已通过 |
| M6 内容与体验   | 未完成               | 只有资产 Manifest、本地化和部分 UI 状态基础，正式资产、音频、教程、目标追踪、内容下限与可用性验收均未闭环                                                                  |
| M7 稳定与 Alpha | 未完成               | 已有性能、安全、备份恢复和运维测试基础，但没有证据表明 200 并发、50 写请求 / 秒、监控告警、恢复演练与 Alpha 门禁全部通过                                                   |

## 已形成的工程资产

- 4 个应用：玩家 Web、API、Worker、Admin 包入口。
- 7 个共享包：契约、数据库、规则、配置、可观测性、测试夹具和 UI。
- API 已注册认证、角色、内容、资产、Buff、装备、淬炼、工具分配、秘境、洞府、筑基、幂等、健康检查、队列和结算模块。
- 数据库迁移已从 `0001_initial` 演进至 `0013_breakthrough_v1`。
- 当前源码测试文件共 87 个，覆盖应用、共享包、资产、集成、E2E 环境、性能、安全和运维工具；全量单测当前为 309 个。
- Web 已有洞府、百艺、历练、角色装备 / 淬炼 / 工具和背包路由；修炼页仍为占位页。

## 2026-08-17 工作记录

### 已有开发成果审计

- 审阅 3 个本地提交：
  - `72a2824 feat: establish playable idle cultivation vertical slice`
  - `cbc8d95 feat: expand production content and add tool assignments`
  - `7bf74f8 feat: add cave tempering and production readiness foundations`
- 审阅未提交开发现场，确认主要新增内容为筑基 API / 数据库 / Worker 恢复、洞府 Web 页面、资产 Manifest 校验、OpenAPI 契约扩展和 E2E 数据库启动器增强。
- 对照 M0–M7 里程碑判断当前真实进度为“M5 进行中”，没有把测试基础或占位入口计作 M6 / M7 完成。

### 公开仓库准备

- 重写过时的 README，使其反映当前纵向切片、技术栈、运行方式、质量命令和已知限制。
- 新建本工作记录，后续任务结束时持续追加日期、改动、验证证据和下一步。
- 增加本次公开发布执行计划：`docs/superpowers/plans/2026-08-17-public-github-release.md`。
- 已创建公开仓库：<https://github.com/hx876298682-tech/dongtian>。
- 已提交并推送 `6b5bc80f0efca7b09f1c948ba5e929c96e421ea9`（`feat: add breakthrough flow and release readiness`）。
- GitHub API 核验：可见性 `public`，默认分支 `main`，远端提交与本地一致；本地 `main` 正跟踪 `origin/main`。

### 验证记录

2026-08-17 公开发布前检查结果：

| 检查                      | 结果         | 证据 / 说明                                                                                                 |
| ------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------- |
| 敏感信息扫描              | 通过         | 未命中私钥、GitHub Token、AWS Key、OpenAI Key、Slack Token 等模式；`.env`、依赖、构建产物和测试结果均被忽略 |
| 大文件检查                | 通过         | 待发布文件没有超过 5 MB 的文件；唯一超过阈值的是被忽略的 `.pnpm-store/v11/index.db`                         |
| `git diff --check`        | 通过         | 无空白错误                                                                                                  |
| `pnpm lint`               | 通过         | ESLint 通过                                                                                                 |
| `pnpm typecheck`          | 通过         | 11 个 workspace 项目全部通过                                                                                |
| `pnpm test`               | 通过         | 81 个测试文件、274 个测试通过                                                                               |
| `pnpm build`              | 通过         | API、Worker、Web 和共享包构建通过；Web 仅有 bundle 大小提示                                                 |
| `pnpm asset:typecheck`    | 通过         | 资产校验工具 TypeScript 检查通过                                                                            |
| `pnpm test:assets`        | 通过         | 1 个文件、4 个测试通过                                                                                      |
| `pnpm asset:validate:dev` | 通过（降级） | 22 个占位 / 缺失资源以 44 条警告保留在开发模式                                                              |
| `pnpm asset:validate`     | 阻断         | release 模式拒绝 22 个占位区域、物品、Buff 和音频条目；这是 M6 资产未完成的预期发布阻断                     |
| `pnpm openapi:check`      | 通过         | OpenAPI 3.1，42 条路径，生成契约兼容                                                                        |
| `pnpm format:check`       | 未通过       | 216 个历史 / 现有文件不符合 Prettier；本轮只格式化 README、工作记录和发布计划，避免全仓无关重排             |
| 浏览器 E2E                | 未运行       | 依赖本机 Chromium、Docker Compose 和 PostgreSQL；当前记录不把它计为已验证                                   |

### 本轮修复详情

- 修复筑基测试的 `beforeEach` / `afterEach` 导入和微秒字符串夹具转换。
- 修复筑基写操作缺失幂等响应包装、camelCase 规则结果未转换为 snake_case REST 响应、字符串微秒错误运算，以及筑基模块缺少资产 / 配置模块导入。
- 修复数据库筑基 Repository 的 `pg` 查询参数类型和未使用辅助函数。
- 补齐 Worker 筑基恢复循环的测试夹具和调用断言。
- 重新生成并检查 OpenAPI JSON / TypeScript 契约。

### 2026-08-17 M5-006 / M5-007 / M5-008 收尾记录

- 由多个 Luna 子代理并行实现并由主代理复核：三槽 `UNTIL_INVENTORY` 条件队列、筑基玩家端闭环和真实数据库 E2E 规格。
- 队列结算修复：每完成一个周期重新评估库存条件并及时截断；输入资产通过 reservation 部分消费后再写入产出；COUNT / DURATION 按剩余目标截断并持久化进度；`BLOCKED + FALLBACK` 切换保底行动；segment 使用新活动队列项；替换队列保留完成审计且不重复插入 RUNNING 项。
- 筑基 Web 修复：`/cultivation` 条件、资源预留确认、试炼路线、完成 / 放弃 / 断线恢复、幂等重试、四类门槛、来源路由和完成后三槽队列编辑均已接入；官方模板使用真实配置 action ID。
- E2E 规格覆盖：黄金路径断线恢复、加速账号毕业与第三药力槽、独立过期释放；账本字段、重复 finalize 幂等、热更快照边界均有断言。
- 主代理全量验证：`pnpm lint` 通过；`pnpm typecheck` 通过；`pnpm test` 通过（85 个测试文件、299 个测试）；`pnpm build` 通过；`pnpm openapi:check` 通过（42 条路径）；资产 typecheck、资产测试和开发模式校验通过。
- E2E 静态验证：ESLint 和 Playwright `--list` 通过，列出 3 条筑基场景；首次默认启动器受本机 Docker / PostgreSQL 环境影响，随后切换外部数据库模式完成真实验收。
- 浏览器补充检查：本地 Vite 未连接 API 时正确显示网络错误重试态；真实数据库模式下三条筑基场景已由 Chrome 完成端到端验证。
- 已提交并推送 `b36d2e79b80fb0c9284832e1952550cd6cf64e8f`（`feat: complete breakthrough and conditional queues`）；GitHub 核验仍为公开仓库、默认分支 `main`，远端提交与本地一致。

### 2026-08-17 真实数据库与浏览器验收补记

- 使用临时 PostgreSQL `18.6`（端口 `5433`）和本机 Google Chrome 执行 `tests/e2e/breakthrough.spec.ts`，3 条筑基场景全部通过（`3 passed`）。
- 本轮修复并加入回归测试：新库迁移先创建 `BreakthroughStatus` 枚举；E2E Web 启动参数改为直接传递 Vite host；Playwright 支持 `PLAYWRIGHT_EXECUTABLE_PATH`；修正 reservation snapshot、`201 Created` 与 PostgreSQL DECIMAL 断言；队列 / 结算 / 数据库边界规范化 DECIMAL 数量；事务 `PoolClient` 的库存查询改为顺序执行。
- 追加收口修复：`UNTIL_INVENTORY` 至少预留一周期输入；结算周期边界按虚拟库存截断并在材料不足时进入 `BLOCKED` / fallback；`SKIP` 转 `DONE_INCOMPLETE`；已满足条件的运行项零时间完成；reservation DECIMAL 部分消费返回精确余量；队列状态 SQL 显式 cast `QueueEntryStatus`；队列替换完成后清除 pending 标记。
- 全量门禁复跑通过：`pnpm lint`、`pnpm typecheck`、`pnpm test`（87 个测试文件、309 个测试）、`pnpm build`、`pnpm openapi:generate`、`pnpm openapi:check`（42 条路径）、`pnpm asset:typecheck`、`pnpm test:assets`、`pnpm asset:validate:dev`；E2E 相关 ESLint 通过。
- 真实 E2E 使用外部数据库模式：`E2E_DATABASE_MODE=external`、`E2E_DATABASE_WIPE_ALLOWLIST=dongtian_test`、`TEST_DATABASE_URL=postgresql://dongtian_test@127.0.0.1:5433/dongtian_test`，并指定 Chrome 可执行文件。
- 验收结束后应停止临时 PostgreSQL 实例；开发模式资产仍有 44 条占位 / 缺失警告，但没有 release blocker。

### 已知风险与限制

- M5 真实 PostgreSQL/Chrome E2E 已通过；后续仍需在 CI / Docker Compose 环境重复执行，避免只依赖本机工具链。
- 当前项目没有 GitHub Actions 工作流，公开仓库创建后仍需补 CI。
- 全量浏览器 E2E 依赖 Chromium 和可用的 PostgreSQL / Docker 环境；若本机环境不满足，必须如实记录未验证项。
- 未添加开源许可证。仓库公开后默认仍保留全部版权，不代表开源授权。

### 2026-08-17 本地体验环境

- 已用临时 PostgreSQL 18（`127.0.0.1:5433/dongtian_test`）完成迁移和 seed，并启动 API 与 Web。
- 当前体验地址：`http://127.0.0.1:5173/`；API 健康检查：`http://127.0.0.1:3000/api/v1/health/live`。
- 本地 Node 为 `22.22.3`，仓库声明 Node `24.x`，启动时只有 engine warning；Docker 未安装，因此本次使用外部 PostgreSQL 模式。
- 可体验范围：M5 筑基流程、队列、背包、生产 / 历练等已有纵向切片；正式美术、音频、完整教程、目标追踪和 Alpha 发布门禁尚未完成。

### 2026-08-17 银河奶牛底层逻辑对照结论

- 当前实现保留了银河奶牛式的核心骨架：时间是主要价值单位；行动队列负责未来规划；在线 / 离线使用同一权威结算器；生产输入、输出、装备修正、Buff、秘境和永久成长共同影响挂机收益。
- 当前实现不是银河奶牛的 1:1 复刻：主题改为修仙，技能 XP 改为修为 / 百艺 XP，境界突破替代等级阶段，秘境与洞府替代部分战斗 / Housing 结构；市场、行会和完整经济 Sink 按 DEC-029 延后。
- 因此“底层时间与队列逻辑没有意外改变”，但目前的实际游玩感受会更像“修仙主题时间队列 + 境界门槛”的纵向切片，还没有原作完整的多技能乘区、市场流通和长期经济循环；这是当前范围缺口，不应误判为完成度等同于银河奶牛。

## 建议下一步

### 2026-08-17 后端纵向流程审计补记

- 修复 `tests/e2e/vertical-slice.spec.ts` 的新会话初始化竞态：`getSession` 现在以 30 秒超时轮询 `/api/v1/auth/session`，确认 `200 + authenticated=true` 后再读取完整会话；保留最终认证断言，不放宽产品行为。
- 验证：该 E2E 文件 ESLint 通过；Playwright `--list` 列出 2 条纵向场景。
- 环境限制：本机 PostgreSQL 18.6 可用，但 Docker 和 Chromium/Chrome 可执行文件不可用，本轮未执行浏览器 E2E。
- 审计风险：`packages/database/prisma/seed.ts` 当前只执行 `SELECT 1`，不会填充 `config_releases`；当前 API 运行时从文件加载配置且未读取该表，因此未将其扩大为本次修复，后续应补 seed 与数据库记录一致性测试。

1. 为公开仓库补 GitHub Actions，至少执行格式、Lint、类型、单测、构建、资产和 OpenAPI 门禁，并在 CI 重复真实 E2E。
2. 再进入 M6 内容扩量、正式资产、教程和可用性测试，不提前宣称 Alpha 可用。

### 2026-08-17 完整游玩流程收尾补记

- Web 端已补齐单人流程导航：洞府、洞府设施、修炼 / 筑基、闭关队列、背包、秘境历练；新增 starter kit 后新匿名角色可直接获得基础装备、工具、默认装备预设和安全策略。
- 秘境机会契约新增 `active_loadout_preset_id`，秘境页会从后端自动采用当前预设；修复 URL 草稿同步非幂等导致的路由反复重置，以及禁用 TanStack Query 被误判为页面永久加载的问题。
- 三槽条件队列继续保留 `UNTIL_INVENTORY`、`BLOCKED + SKIP/FALLBACK` 和 reservation 释放回归覆盖。
- 本轮定向门禁通过：6 个测试文件、22 个测试；`pnpm typecheck`；`pnpm lint`。
- 真实 Chrome / PostgreSQL 纵向 E2E 已运行并暴露秘境页加载状态问题；接口响应本身为 HTTP 200，页面查询状态的完整修复已加入，但本轮尚未重新取得最终 `vertical-slice` 通过证据，因此不把完整浏览器流程标记为已验收。
- 当前可体验入口仍为 `http://127.0.0.1:5173/`（需先启动 PostgreSQL 5433、API 3000 和 Web 5173）；正式美术、音频、教程和经济系统扩展仍未纳入。
- 新增 macOS 一键体验入口 `启动洞天.command`：双击后自动检查 PostgreSQL / pnpm、准备本地 `dongtian` 数据库、执行迁移和种子、启动 API/Web 并打开浏览器；按键退出时清理本次启动的服务。已通过 `zsh -n` 语法检查。
- 启动器现在会先按端口检查 3000/5173 的监听进程，只清理工作目录属于本项目的残留 API/Web 进程；若端口属于其他项目则安全中止，不会误杀。清理后自动启动并打开浏览器。
- 根据实际本机环境修正数据库探测：启动器优先使用 5432，若本机 PostgreSQL 运行在 5433，则自动切换到 `dongtian_test` / `dongtian_test` 连接，不再因默认端口不存在而直接退出。
- 修复一键启动器 Web 地址无法访问：Vite 启动参数不再带多余的 `--`，明确绑定 `127.0.0.1:5173`；启动器会在打开浏览器前轮询确认 API 和 Web 都返回成功。残留进程识别也覆盖 `apps/web` 子目录。

### 2026-08-17 单视口游戏界面改造

- 用户确认所有页面必须按游戏界面开发：禁止 `html` / `body` / 路由根节点产生页面级纵向滚动；桌面保留顶栏、左导航、主区、右栏和底栏，移动保留顶栏、主区和底部导航；超量内容只允许在主内容或明确局部面板内部滚动。
- 强制规范已写入 `docs/开发前文档包/07_UI信息架构与核心流程线框_V1.0.md` 第 19 节，包含 MUST、禁止项、目标视口、验收标准和 Release Blocker 规则。
- Web 壳层已改为 `100dvh` 固定视口网格，所有关键 grid 子项补齐 `min-height: 0` 和内部滚动边界；1366px 桌面宽度不再把右栏堆叠到页面下方；新增移动端底部主导航。
- TDD 回归：`apps/web/src/app.test.tsx` 先确认固定视口、内部滚动和移动导航断言失败，完成实现后 3 项通过。
- Web 全量验证：25 个测试文件、56 个测试通过；Web typecheck、ESLint、Vite build 通过。构建仍提示现有主包大于 500 kB，未影响本轮验收。
- 真实浏览器矩阵：洞府、洞府设施、修炼、百艺、秘境、角色、背包 7 个路由在 1366x768 和 375x667 下 `document/body` 溢出均为 0；1440x900、390x844 亦通过；控制台无 error/warn。

### 2026-08-17 可发布开发版补强

- 复核后确认离线摘要 API 实际已经存在，缺口在 Web 适配层：现在洞府首页会把真实 `rewards`、`consumptions`、`timeline`、`anomalies` 接入权威摘要，不再显示“后端尚未提供 GET”的占位说明；`settlement: null` 保持明确空态。
- 洞府目标追踪新增筑基条件接口读取，展示已满足条件数、资源缺口和是否可开始筑基；接口失败时降级为目标不可用，不伪造完成状态。
- M6 内容下限已由当前配置满足：6 个区域、11 个妖兽（至少 2 个 Boss）、3 个秘境、25 个配方；配置回归测试固定这些下限并通过配置校验。
- 新增 GitHub Actions CI：依赖安装、lint、typecheck、全量测试、集成测试、OpenAPI、build 和 Playwright smoke discovery；新增 `pnpm test:e2e:smoke`。
- 本轮门禁：全量 Vitest 89 个文件 / 321 个测试通过；typecheck、lint、build、config validate、OpenAPI check、integration tests 通过。Node 22 相对仓库要求 Node 24 仍有 engine warning。
- 尚未冒充完成的阻塞：教程规则已有 game-rules，但缺少角色教程进度持久化表和 API，因此暂不显示“已完成教程”；需要下一轮补齐事件投影/进度读写后才能完成教程 UX 闭环。市场、宗门、社交、正式音画按既定范围继续排除。

### 2026-08-17 可玩开发版最终验收

- 修复秘境运行记录 INSERT 的列和值占位符数量错误；数据库包重建后真实进入秘境成功。
- 统一秘境运行读取、选路、结算客户端路径为 `/api/v1/characters/dungeon-runs/...`，与 API Controller 实际路由一致；同步契约夹具。
- 纵向 E2E 修正为验证概率战斗下的稳定结算与背包响应，不把 62% 成功率错误写成必得掉落；真实 Chrome + PostgreSQL 流程 `1 passed (7.8s)`。
- 全量验证：Vitest `89 files / 321 tests passed`；集成测试 `3 files / 6 tests passed`；typecheck、lint、build、config validate、OpenAPI check 全部通过。
- 已知环境提示：本机 Node `22.22.3`，仓库要求 Node `24.x`，仅有 engine warning；不影响本轮验证结果。
- 本次范围已完成至可体验开发版：排队、离线结算、修炼 / 筑基、生产、背包、秘境进入 / 恢复 / 选路 / 撤离 / 结算、装备预设和单视口 UI 均可串联体验。
- 继续排除：正式美术、音频、市场 / 交易、宗门 / 公会、社交 / 多人、排行榜和完整经济 Sink；教程进度持久化仍未伪造完成，待后续系统开发。

### 2026-08-17 首页银河奶牛式体验改造

- 首页主循环调整为“选任务 → 立即开始挂机 → 离开一会儿 → 回来领取离线收益”，不再要求玩家先理解队列编辑、预览和保存。
- 新增三个首屏任务卡：灵气修炼、采集青灵草、炼制聚气丹；点击后直接调用保存计划接口并自动恢复暂停中的挂机队列。
- 将行动 ID、模式枚举、队列版本、结算 ID、状态枚举、微秒和 JSON 输入输出等工程字段从玩家主界面文案中移除或改为中文游戏语义。
- 高级挂机计划编辑器保留为二级设置，首屏不再以“队列编辑器”作为开始游戏入口。
- 更新 E2E 断言并通过真实 Chrome + PostgreSQL 纵向流程：`1 passed (7.7s)`；全量 Vitest：`89 files / 322 tests passed`；typecheck、lint、build 通过。
- 发现并处理重复 React key，当前真实流程无对应浏览器 console 警告；Node `22.22.3` 的 `24.x` engine warning 仍为环境提示。

### 2026-08-17 挂机行为进度反馈

- 首页新增当前挂机进度条：显示本轮百分比、剩余时间和已完成轮数，进度以服务端 `progress_time_us` 和队列快照时间为基准，每秒平滑更新。
- 刷新后会从服务端快照恢复进度；暂停时冻结本地推进；刚保存但尚未进入 RUNNING 的队首任务也会立即显示进度，避免点击任务后页面没有反馈。
- 新增进度计算单元测试与 E2E `progressbar` 断言；真实 Chrome + PostgreSQL 纵向流程 `1 passed (8.0s)`，全量测试 `89 files / 322 tests passed`。

### 2026-08-17 顶部进度栏与开发热更新

- 将当前挂机进度栏移动到首页内容区第一项，并设置为 sticky；滚动首页内部内容时仍可看到当前任务、剩余时间、本轮百分比和累计轮数。
- Web 继续使用 Vite HMR；API 开发脚本从一次性 `tsx src/main.ts` 改为 `tsx watch --clear-screen=false src/main.ts`，启动窗口保持打开时前后端源码保存后会自动更新 / 重启。
- `启动洞天.command` 继续作为首次启动入口，无需每次代码更新后重新双击；仅在关闭启动窗口、服务异常退出或修改数据库迁移 / 环境变量时需要重启。
- 验证：Web/API typecheck、Dashboard 测试 `2 files / 9 tests`、全仓 lint 通过；本机 `tsx watch` 命令可用。

### 2026-08-17 全局顶栏挂机进度

- 根据截图反馈，将挂机进度从洞府内容区移动到 App Shell 全局顶栏，位于品牌区和页面状态区之间；切换修炼、百艺、历练、角色、背包等页面时始终可见。
- 全局栏显示当前任务、进度条、百分比、剩余时间和已完成轮数；每秒本地平滑更新，每 30 秒重新读取队列快照，刷新后继续以服务端时间为准。
- 首页开始新任务后同步失效全局队列查询，确保顶栏立即切换到新任务。
- 验证：Web typecheck、App / Dashboard 测试 `2 files / 8 tests`、全仓 lint 通过。

### 2026-08-17 银河奶牛式 UI 结构对齐

- 使用用户已登录的银河奶牛挂机页面进行只读结构对照，确认其核心空间关系：固定三栏、顶部当前行为、左侧技能 / 任务导航、中央任务工作区、右侧角色背包信息。
- 本项目 App Shell 对齐为紧凑深色游戏面板：桌面端 `240px / 自适应中央 / 360px` 三栏，小圆角、紧凑间距、深色导航和右栏；移动端保持原有响应式收缩。
- 顶栏继续使用我们自己的洞天 / 修炼数据，右栏文案从“闭关与草稿、工程槽位”改为“角色与背包、正在进行、最近收获、下一境界、快捷任务”。
- 洞府首页移除重复的页面内挂机进度条和工程摘要指标，保留全局顶栏作为唯一挂机状态入口；任务卡保留修仙内容但改为深色紧凑游戏卡。
- 明确不复制对方品牌、角色素材、图标或源码；当前对齐的是公开可见的布局和交互层级，素材与修仙内容继续使用项目自己的实现。
- 浏览器截图验证：本地页面已呈现全局顶栏进度、左导航、中央任务卡、右角色栏的统一游戏壳；Web typecheck、App / Dashboard 测试 `3 files / 12 tests`、全仓 lint 通过。
- 右栏补充紧凑背包摘要网格，读取我们自己的货币和物品数据；不复用参考游戏素材，只借鉴其“数量 + 图标位 + 分类网格”的信息密度。
- 当前已建立完整复刻清单：主挂机、技能/生产、背包/装备、秘境战斗、角色突破、通用弹窗和右栏角色信息按 P0 顺序继续推进；任务、成就、图鉴、坊市、宗门、社交等缺少真实后端能力的页面不会伪造完成状态。
- 全局顶栏补齐“停止”当前挂机操作，并在成功后同步刷新顶栏与洞府队列；中央内容区底部增加可收起的修行日志，展示当前行动、最近收获和下一目标，模拟参考游戏常驻聊天/日志区但不伪造社交消息。
- 顶栏移除 CSRF、状态版本等工程字段，替换为所在区域和角色；所有现有页面面板、标签、列表、弹窗统一为紧凑深色、小圆角视觉基线。
- 百艺页、背包页、角色装备页和秘境页已完成第一轮参考式重排：技能 / 炼丹标签、物品网格、装备方案、出战路线和探险结果均保留现有修仙 API 操作，页面内原始 ID / 版本 / 权威快照文案已降级为玩家语言。
- 洞天设施和秘境的加载、错误、准备、运行状态也已改为玩家语言；对应组件测试同步更新并通过。
- 角色装备页进一步移除 `preset_id`、状态版本、装备实例和服务端校验等工程文案，改为装备方案、背包、淬炼和属性比较的玩家语言；状态测试同步通过。
- 修炼 / 筑基页的加载、错误、条件和成功状态改为突破流程语言，隐藏权威快照、原子更新等实现细节；状态测试同步通过。
- 新增设置页 `/settings`：可操作切换紧凑面板、减少动效和显示修行日志，设置持久化在当前浏览器并立即作用于游戏壳。
- 新增全局游戏反馈 Toast：暂停挂机、开始任务、装备保存/启用、洞天升级、秘境进入/选路/结算都会在顶栏下方给出统一反馈，避免操作后只能看接口状态或等待页面刷新。
- 背包物品详情继续清理原始资产类型和 ID 展示，改为修行物品名称、分类、数量、来源和用途。
- 本轮全量回归：Vitest `89 files / 322 tests passed`；全仓 typecheck、lint、build、配置校验、OpenAPI 检查通过；导航测试已覆盖新增设置页。
- 按完整参考页面清单新增任务、迷宫、商店、成就、排行榜、宗门、仙友、指南、规则和更新入口；任务 / 迷宫 / 指南 / 规则接入现有修仙内容，市场经济、多人、成就统计等缺后端的页面显示真实锁定态，不伪造交易、排名或玩家消息。
- 本地路由矩阵已验证以上 10 个页面均可打开，所有页面共享全局挂机栏。
- 新增共享 `GameDialog`：参考式页面的分类详情、延期说明和角色数据摘要现在通过统一浮层打开 / 关闭，已在任务页验证对话框、标题和关闭按钮可用。
- 右栏改为可切换的“背包 / 装备 / 技能 / 洞府 / 配装”标签面板，使用现有修仙路由和数据；切换标签不会离开当前页面，当前挂机和收获摘要保持常驻。
- 右栏标签实测：切到“装备”显示打开装备入口，返回“背包”显示物品网格；全局挂机栏和当前行动摘要不受切换影响。
- 底部修行日志改为可切换的“收获 / 战斗 / 系统”频道；战斗无记录时显示真实空态，系统频道展示连接和设置提示；浏览器实测频道切换成功。
- 任务页接入全局队列并显示真实当前行动名称；迷宫页升级为“迷宫 / 房间 / 自动化”三标签，读取真实秘境机会，房间与自动化入口连接现有秘境流程。
- 浏览器实测：迷宫入口、房间列表、自动化编辑入口均可切换；任务页显示当前行动“百草谷采药”。
- 设置页补齐“个人资料 / 游戏 / 聊天 / 通知 / 账户”五标签，紧凑布局、减少动效、日志显示、时间标签、桌面通知和重要操作确认均真实持久化到本地设置；浏览器实测聊天、通知、账户标签切换成功。
- 修复完整导航在移动端由 6 项增长到 17 项后的多行挤压：改为单行横向滚动；新增参考页面和设置标签在窄屏下也改为横向滚动，任务/指南卡改为单列。

### 2026-08-17 挂机 / 秘境 parity 补强

- 首页新增当前挂机任务详情弹窗和最近结算奖励状态弹窗，均从真实队列条目与结算响应读取，状态和时间转换为玩家语言。
- 队列操作保留真实保存、暂停、恢复 API；后端契约没有停止队列接口，因此“停止”明确锁定并标注未开放，不伪造请求。
- 秘境运行页新增房间详情与自动化策略弹窗，读取当前节点、已选路线和策略预设；房间、路线和策略均做中文映射，未开放配置保持只读说明。
- 将秘境结算按钮改为“结算收获”，避免展示实现层 finalize 术语。
- TDD 验证：先确认新增弹窗测试失败，再实现；目标测试 `2 files / 8 tests passed`，Web typecheck、全仓 lint、`git diff --check` 通过。Node `22.22.3` 的 `24.x` engine warning 仍为环境提示。

### 2026-08-17 parity 审核与一屏回归

- 顶栏境界和修为补充玩家文案映射与数值格式化：`realm.mortal.entry` 显示为“炼气入门”，修为不再透出数据库六位小数。
- 任务详情按钮改为 `current` 为空时回退队首任务，确保真实队列存在时弹窗可打开；新增回归测试覆盖该 API 边界。
- 装备详情清理“服务端未提供”文案，改为“品质：未鉴定”；秘境弹窗删除重复分支，停止按钮提示改为玩家语言。
- 移动端修复 grid 最小宽度导致的 668px 横向裁切：`.app-shell` 使用 `minmax(0, 1fr)`，`.shell-main` 明确允许收缩。
- 浏览器矩阵：桌面 17 条路由页面级滚动高度等于视口；任务详情弹窗可打开/关闭，右栏装备标签和活动日志可切换；375×667 移动端 `document/body` 宽高均不溢出。
- 全量门禁：Vitest `89 files / 329 tests passed`、typecheck、lint、build、OpenAPI check、`git diff --check` 均通过。Node `22.22.3` 的 `24.x` engine warning 和既有 bundle 大小提示仍存在。

### 2026-08-17 右栏角色摘要 parity 补强

- 右栏“战利品 / 装备 / 技能 / 洞府 / 配装”五个标签接入真实 contracts 数据，分别显示库存网格、三装备槽位、技能等级与修为进度、洞府设施等级状态、当前出战方案和补给。
- 新增 `right-rail-adapter` 纯函数与 TDD 测试，物品 / 技能 / 装备名称做玩家语言映射，右栏不显示 raw ID、配置版本、实例字段或 API 元数据；原有装备、百艺、洞府、角色跳转入口保留。
- 验证：右栏 adapter 与 App shell 目标测试 `2 files / 11 tests passed`；Web typecheck、定向 ESLint、`git diff --check` 通过。Node `22.22.3` 相对仓库 `24.x` 仅有 engine warning。
- 质量审核修复：洞府设施按 `facility_kind` / 配置键映射为聚灵室、炼丹房、炼器房，未知设施显示“未识别设施”；未知技能显示“未知技能”；库存摘要新增稳定 `key`，App 使用该 key 避免重复 React key。回归目标测试 `2 files / 17 tests passed`，定向 ESLint 与 `git diff --check` 通过。

### 2026-08-17 参考页交互深度补强

- 任务、迷宫、指南、规则、更新页改为玩家语言的筛选条、条目列表、明确空态和条目详情 `GameDialog`；任务与迷宫仍读取真实队列 / 秘境机会数据。
- 商店、成就、排行榜、宗门、仙友改为参考式锁定面板，说明对应后端未开放且不伪造商品、排名、成员或消息；锁定操作明确禁用。
- 新增参考页源契约测试，先验证失败再实现；目标测试 `1 file / 3 tests passed`，仓库 lint、定向 ESLint、`git diff --check` 通过。
- Web typecheck 被既有 `apps/web/src/features/system/right-rail-adapter.ts` 中未使用的 `assignments` 变量阻断，本轮未改动该无关文件。

### 2026-08-17 重要操作确认设置接入

- 新增 `game-settings` 容错读取 helper，`confirmImportantActions` 缺失或存储损坏时默认开启；先红后绿测试通过。
- 突破开始 / 完成 / 放弃、洞府开建、装备淬炼、秘境结算接入该设置：开启时使用确认浮层，关闭时直接调用既有真实 mutation；未新增后端操作。
- `GameDialog` 新增 `ImportantActionDialog` 轻量包装，秘境当前没有实际退出 API，因此未伪造退出按钮。
- 定向测试 3/3、Web typecheck、相关 ESLint、`git diff --check` 通过；Node 22 相对仓库 24 仅有 engine warning。

### 2026-08-17 P0 全局壳 parity 收口

- 左侧桌面导航保留五组和全部现有路由，入口收敛为无说明句的紧凑“标记 + 名称”；没有真实可用等级数据时不展示伪造等级或状态。
- 删除主壳多余 `app-shell__footer`，修行日志保留在主区 grid 的底部边界；移动端底部导航、固定视口和内部滚动规则保持不变。
- 右栏保留真实数据摘要和“战利品 / 装备 / 技能 / 洞府 / 配装”五标签，移除无实际玩法价值的固定 / 取消固定操作。
- TDD：先写壳层反向断言并确认红灯，最小实现后 `app.test.tsx` 全部通过（92 个文件 / 342 个测试）；Web typecheck、定向 ESLint、`git diff --check` 通过。

### 2026-08-17 P1/P2 全局壳质量收口

- 移动端 `<=920px` 顶栏父级真实 `flex-wrap`，挂机条、资源摘要和子项补齐 `min-width: 0` / `max-width: 100%`，并增加静态样式断言覆盖 375px 无横向裁切所需约束。
- 右栏装备、洞府、配装分别明确 loading、error、真实空态；错误态提供“重试”，按对应 `global-*` query key invalidate；移除用 `?? null` 直接伪装未装备 / 暂无配装的渲染路径。顶栏库存摘要复用 `global-inventory` query key，减少重复请求。
- 洞府设施摘要按真实建设任务状态显示“建设中 / 已完成 / 运行中”，并补充 adapter 回归测试。
- 左栏折叠按钮补充 `aria-expanded` / `aria-controls`；右栏五标签采用 `tablist` / `tab` / `aria-selected` / `tabpanel`，修行日志频道补充 `aria-pressed`。
- TDD 红绿证据：新增断言先在旧实现下失败，修复后定向 Vitest `2 files / 19 tests passed`；Web typecheck、build、定向 ESLint、`git diff --check` 全部通过。build 仅保留既有 bundle 大小 warning。
- 全量测试输出仍包含并行代理正在处理的装备详情与 content-adapter 既有契约失败，本轮未覆盖或回退其改动。
- 进一步收紧右栏 adapter：装备 / 配装摘要函数只接受真实 preset，暂无数据必须由显式空态处理，避免未来调用重新把 `null` 转成“未装备 / 暂无配装”。

### 2026-08-17 P1/P2 退回项状态语义修复

- 修复右栏 tab 的无效 ARIA 关联：仅当前渲染的 tab 设置 `aria-controls`，避免非当前 tab 指向不存在的 panel。
- 背包摘要区区分 query 错误、加载中和真实数据；错误时显示“背包暂时无法读取”并可按 `global-inventory` 重试，未返回数据时不再显示伪造的“0件”；真实空库存仍显示真实数量。
- 技能摘要区同时等待 progression 和 tool assignments，增加错误重试和“暂无修行技能”空态；两个 query 未完成时显示加载态，不再用半成品数据渲染技能。
- 洞府设施未知建设状态显示“状态未知”；没有建设任务的设施仍保留合理默认“运行中”，避免把未来状态误判为运行中。
- TDD 回归：先确认退回断言 3 项失败，再实现并通过定向 Vitest `2 files / 19 tests passed`；Web typecheck、定向 ESLint、build、`git diff --check` 全部通过。build 仅保留既有 bundle 大小 warning，Node `22.22.3` 相对仓库 `24.x` 仅有 engine warning。
- 追加 P2 空库存修复：真实 `total_count === 0` 时右栏显示“背包暂时为空”及明确说明，不再只显示“0件”与空网格；先确认新增断言红灯，再实现并恢复定向 Vitest `2 files / 19 tests passed`，其余门禁保持通过。

### 2026-08-17 P0 玩家文案清理（本子任务）

- 筑基、洞府、装备 / 淬炼、百艺工具和秘境页面隐藏服务端、权威、配置 / 状态版本、运行标识、时间戳、审计 / 事务标识和原始错误；改用“刷新状态”“暂时无法读取，请稍后重试”“由洞天规则结算”等玩家语言，真实请求字段与重试逻辑保持不变。
- 洞府空态改为“洞府暂未发现设施”；参考页与本机草稿提示移除持久化 / 版本工程术语；首页 boot 文案改为基础状态与维护提示。
- TDD 回归：先确认旧文案断言红灯，再更新最小渲染契约；定向 Vitest `6 files / 9 tests passed`，Web typecheck、定向 ESLint、`git diff --check` 通过。Node `22.22.3` 相对仓库 `24.x` 仅有 engine warning。
- 追加收口内容页与装备页残留文案：内容列表 / 维护态不再显示“权威 / API / 原始错误”，装备页不再显示恢复权威、状态 / 配置版本或 `effective_next_cycle` 字段。
- 追加定向验证：`7 files / 15 tests passed`，Web typecheck、相关 ESLint、`git diff --check` 通过。

### 2026-08-17 快速挂机与洞府页面真实流程收口

- 修复首页点击“开始灵气修炼”稳定返回 409 的根因：首次队列变更会先结算离线保底修炼，结算段超过默认 100 段后在同一事务内标记续算，随后队列锁拒绝写入。队列保存 / 暂停 / 恢复现在使用足够覆盖离线窗口的结算段额度（10,000），保留真实事务与续算保护，不在前端吞掉冲突。
- 新增队列服务回归测试，验证保存与暂停都会传递统一的结算段额度；移除所有临时 `[queue-debug]` 诊断日志。
- 修复洞府 API 配置索引错误：设施目录使用设施族 ID，而配置注册表按等级实例 ID 索引；现在按设施族读取 1 级定义作为展示元数据，洞府页不再因 `CONFIG_NOT_FOUND` 返回 500。
- 洞府适配层和页面进一步清理玩家可见 raw ID：材料显示中文名称，境界显示中文阶段，设施效率显示为修为 / 炼丹 / 炼器百分比加成，建设成本快照不再显示配置键。
- 浏览器真实验证：点击快速挂机后顶部显示“采气修炼”进度条和“暂停挂机”；暂停后显示“恢复挂机”，恢复后回到“暂停挂机”；洞府页能加载聚灵室、炼丹房、炼器房。
- 19 条本地路由重新巡检，均无页面错误和工程化玩家文案（包括 `item.*`、`realm.*`、`TUT-*`、API / CSRF / 幂等 / 实例字段等）。
- 最终门禁：Vitest `92 files / 359 tests passed`、typecheck、lint、build、OpenAPI check、`git diff --check` 均通过。仅保留 Node `22.22.3` 相对仓库 `24.x` 的 engine warning 与既有 bundle size warning。

### 2026-08-17 P0 技术键可见输出补充清理

- 百艺工具适配层将 `item.cuizhi_danlu.name` / `item.mubing_yaochu.name` 映射为“粗制丹炉” / “木柄药锄”，将 `alchemy_tool` / `herbalism_tool` / `mining_tool` 映射为玩家可读工具类型；技能、境界和比较摘要不再暴露技术键。
- 秘境适配层将 `TUT-007` 映射为“秘境入门教学”，并隐藏恢复时间、物品 / 路线 ID、战斗微秒值等 raw 输出；奖励与路线改用中文名称。
- TDD 回归：新增 2 个适配器契约测试，先确认旧输出红灯；定向 Vitest `4 files / 7 tests passed`，Web typecheck、相关 ESLint 通过。

### 2026-08-17 Milky Way 全局壳层导航与比例收口

- 左侧导航保留 `SHELL_ROUTES` 全部页面入口，商店入口按玩家语言显示为“市场”；追加从角色 progression API 读取的百艺技能等级，以及真实修为 / 秘境机会驱动的战斗与修炼入口，不硬编码技能 raw ID，也不使用“更多”折叠。
- 顶部全局挂机进度、四个增益槽和资源摘要压缩为更接近参考站的比例；右侧背包 / 装备 / 技能 / 洞府 / 配装仍按当前 tab 只渲染对应真实数据或明确空态；底部聊天继续保持未开放且无伪造消息。
- TDD：新增壳层动态技能与紧凑比例断言，先确认旧实现 RED，再恢复 GREEN；`app.test.tsx` 15/15、Web typecheck、定向 ESLint、`git diff --check` 通过。Node `22.22.3` 相对仓库 `24.x` 仅有 engine warning。

### 2026-08-17 参考页任务与页面族交互收口

- 任务页 tabs 收敛为“任务栏 / 任务商店”；任务栏改为单列紧凑任务行，使用真实队列的目标、状态、进度和结算状态，详情弹窗只展示这些权威字段，不新增领取动作。
- 迷宫页 tabs 收敛为“迷宫 / 迷宫商店”；迷宫页读取真实秘境机会，房间与自动化改为通往 `/expedition` 的真实入口，未伪造房间路线或自动化配置。
- 商店、成就、排行榜、宗门、仙友锁定页保留各自标题、tabs、面板标题和延期说明；指南、规则、更新使用各自内容面板和条目列表，避免单一大卡片空壳。
- TDD 红绿证据：先增加迷宫 tabs、任务奖励状态、紧凑任务行和页面族视图断言，旧实现 7 项中 3 项失败；实现后 focused reference-pages 测试 `7/7`、相关页面测试 `14/14` 通过。
- 全量验证：`pnpm typecheck`、`pnpm lint`、`git diff --check` 通过；Node `22.22.3` 相对仓库声明 `24.x` 仅产生 engine warning。市场商品、成就统计、排行榜、宗门成员、社交消息仍受延期后端限制并保持锁定。

### 2026-08-17 百艺与背包内容页 QA 收口

- 复核百艺 / 背包页面的玩家可见状态文案，移除“页面壳”“渲染”等工程化表达；加载态改为整理修行技能、炼丹内容和储藏信息，错误态改为“暂时无法读取这些内容，请稍后重试”，真实重试入口保持不变。
- 背包物品行不再直接显示 `HERB` / `ORE` 等类别枚举，统一映射为灵草、矿材、丹药等修行分类；技能与修为数值显示为“技能经验 / 修为经验”。
- 百艺、背包的未知详情继续分别提示“未找到对应的行动 / 配方 / 修行物品”，避免把玩家带入原始查询字段语境；同步更新对应契约测试。
- 验证：`apps/web/src/features/content/content-page.test.tsx` 共 `6/6` 通过；Web typecheck 通过；content 页面 ESLint 通过；`git diff --check` 通过。当前 Node `22.22.3` 相对仓库声明 `24.x` 仅产生 engine warning。
- 本轮审查未改变修仙数据、真实 API 或队列行为；市场、宗门、社交、正式美术和音频仍按延期范围排除。

### 2026-08-17 银河奶牛式全页面 parity 审查收口

- 参考页任务与迷宫增加真实 API 错误态、保留旧数据提示和“重试”入口；任务队列每秒刷新，COUNT 按完成轮数 / 目标轮数计算，DURATION 按真实目标时长计算，不再被当前行动周期进度覆盖。
- 参考页 tabs 统一为 `tablist` / `tab` / `tabpanel` 语义，补齐 `aria-selected`、`aria-controls` 和 roving tabindex；商店、成就、排行榜、宗门、仙友使用不同的紧凑锁定面板，延期系统不伪造商品、排名、成员或消息。
- App Shell 移动导航改为完整 `SHELL_ROUTES` 横向入口，17 个页面在窄屏均可访问；顶栏境界、修为、灵石、秘境机会区分读取中、不可用和暂不可用，不再用 0 或初始境界伪装未知状态。
- 右栏五标签补齐 Arrow / Home / End 键盘导航和 overlay Tab 焦点循环；无真实技能 progression 时显示“等级暂不可用”；未接入增益、身份和活动状态明确标注“未接入”；日志 tabs 移除无效 `aria-pressed`。
- 百艺 / 背包继续清理工程化玩家文案，物品类别枚举映射为修行分类；重复工作记录 `WORKLOG.md` 已合并并删除，正式记录保持单一来源。
- 全量质量门禁：Vitest `94 files / 384 tests passed`；`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm openapi:check`、`git diff --check` 全部通过。仅有本机 Node `22.22.3` 相对仓库要求 `24.x` 的 engine warning，以及既有 bundle 大小 warning。
- 浏览器验收：桌面 1728×849 和移动 375×667 的 16 个页面均无 `Unexpected Application Error`；移动页面级 `body/html` 横向溢出为 0，移动导航包含 17 个入口；新 Chrome 会话逐页 console error 为 0；首页可点击任务直接开始挂机，顶部进度条可见，任务详情弹窗可打开关闭，任务商店 / 迷宫商店锁定态、右栏标签和设置标签可切换。
- 当前仍明确排除：正式美术、音频、市场交易、宗门 / 公会、社交 / 多人、排行榜统计和教程进度持久化；这些属于延期或待后端支持的系统，不应在前端伪造完成。

### 2026-08-17 参考页面族路由入口补齐

- 保留既有 `/shops`、`/news` 深链，并在桌面 / 移动完整导航加入独立的 `/store`、`/cowbell-shop`、`/changelog` 入口；市场与新闻标签改为玩家可见的“市场”“新闻”。
- 增加 `/market`、`/updates`、`/update-log` 书签别名，全部复用已有 `shops` / `news` 页面 kind，不新增交易、商店商品或更新服务端数据，不伪造延期系统。
- TDD 回归：先验证新增导航契约红灯，再实现后 `navigation.test.ts` 与 `app.test.tsx` 共 `23/23` 通过，定向 ESLint 与 `git diff --check` 通过。
- 本轮 Web typecheck 未能完成：并行角色改动中的 `tool-assignments-page.test.tsx` 引用了尚未导出的 `summarizeToolInventory`；该阻塞与本路由子任务无关，需主线合并并行改动后重新执行全量门禁。

### 2026-08-17 页面族变体与最终 parity 复核

- 参考站页面族进一步拆开：`/shops` / `/market` 显示“洞天市场”，`/store` 显示“洞天商店”，`/cowbell-shop` 显示“牛铃商店”；`/news` 显示“洞天新闻”，`/changelog` / `/update-log` 显示“更新日志”。这些页面沿用统一锁定交互，但不共享错误标题，也不伪造商品、挂牌、价格或外观数据。
- 迷宫标签现在与参考站一致为“迷宫 / 房间 / 自动化 / 迷宫商店”；房间和自动化条目能打开详情并跳转真实 `/expedition`。商店、成就、排行榜、指南、规则、宗门标签层级按参考站重新核对，长标签只在局部 tablist 内滚动。
- 角色页继续清理玩家可见“实例 / 审计 / 原始物品 ID”表达，比较结果、淬炼结果和工具库存统计统一使用装备 / 物品语言；淬炼重试同样遵守重要操作确认设置。
- 全量门禁重新执行：Vitest `94 files / 391 tests passed`；`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm openapi:check`、`git diff --check` 全部通过。OpenAPI 检查报告 `3.1.0 / 42 paths`；仅有 Node `22.22.3` 相对仓库要求 `24.x` 的 engine warning 与既有 bundle 大小 warning。
- 最终 Chrome 复核覆盖 24 个桌面路由和同一批移动路由：1728×849 与 375×667 下全部无 `Unexpected Application Error`；`body/html` 尺寸分别精确等于视口，页面级横向 / 纵向滚动为 0，新会话逐页 console error 为 0。新增市场、商店、牛铃商店、新闻、更新日志入口均可达。
- 核心交互复核：首页顶部挂机进度持续可见；任务详情、迷宫房间 / 自动化详情和更新日志详情弹窗可打开关闭；右栏与设置标签支持标准键盘导航；市场、商店、宗门、社交、成就、排行等延期系统仍显示明确锁定态。
- 当前剩余范围仍是正式美术、音频以及没有真实后端契约的市场交易、牛铃商品、宗门 / 社交多人、排行榜统计；这些不能通过前端静态内容冒充已完成。

### 2026-08-17 最终玩家文案与全量验收

- 修复排行榜锁定态的可见空态，明确显示“暂无真实排行数据”，保留模式 / 统计类别 tab 和键盘切换，不伪造排名。
- 任务商店、迷宫商店的锁定说明改为玩家语言“还没有可用商品和兑换操作”，移除最后两处“尚未接入”工程表述。
- 定向验收：参考页、App、筑基相关测试共 `6 files / 40 tests passed`；Web typecheck 和相关 ESLint 通过。
- 全量验收：Vitest `94 files / 391 tests passed`；`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm openapi:check`（OpenAPI `3.1.0 / 42 paths`）和 `git diff --check` 均通过。
- 浏览器验收：桌面 `1728×849` 与移动 `375×667` 的 24 个路由全部无应用错误、无工程化玩家文案、页面级 `body/html` 尺寸等于视口；控制台错误为 0。首页启动挂机、顶部全局进度、任务详情弹窗、排行榜分类键盘导航、更新日志详情弹窗均实际操作通过。
- 环境提示仍只有仓库要求 Node `24.x` 而本机为 `22.22.3` 的 engine warning，以及既有 bundle size warning；不影响本轮门禁结果。

### 2026-08-17 百艺行为执行：区域采药闭环

- 百艺左侧技能区新增“采集 / 采药”子入口，进入 `/craft/herbalism` 后按“地图区域 → 区域资源 → 开始采集”执行；地图按 `regions.json` 的六个区域顺序展示，当前没有采药行动的区域保留为不可用态，不再隐藏地图全貌。
- 新增 `apps/web/src/features/behavior/behavior-adapter.ts`，复用已发布区域、行动和物品配置的真实 ID，提供区域查找、采药行动筛选和中文资源映射；行动详情、解锁原因、产出和耗时继续由 `/api/v1/actions` 权威返回。
- 点击可用草药会通过现有队列 API 写入单条 `INFINITE` 行动；若队列版本冲突会重新读取并重试一次，暂停队列会自动恢复，同时失效行为页、洞府首页和全局顶部进度查询并发送统一反馈。行动只有在明确支持无限模式时才显示可采集，避免 API 模式契约不匹配。
- 新增页面状态与队列触发测试，覆盖单条无限队列、暂停恢复、409 重试、查询失效、反馈，以及锁定 / 非无限行动不触发保存；境界展示使用玩家中文标签，不暴露 `realm.*` 原始键。
- 验证：Web 行为 / 导航聚焦测试 `15/15`；Web 全量测试 `32 files / 135 tests passed`；Web typecheck、全仓 ESLint、Web build、`git diff --check` 通过。Node `22.22.3` 相对仓库要求 `24.x` 仍有 engine warning，构建保留既有 bundle size warning。
- 该行为页已形成可复用的“区域 → 资源 → 行动”骨架，后续挖矿、采集其他类别或生产行为可复用同一触发 helper 与页面结构，不需要新增结算内核。
- 导航层级复核纠正：初版误将“采集 / 采药”渲染在“技能等级”区块内；现已把子路由绑定为 `craft.children`，并紧跟“百艺”主菜单渲染，技能等级区块只保留等级数据。新增断言确保采药子菜单归属于百艺路由；修复后 Web 全量测试 `32 files / 135 tests passed`，typecheck、全仓 ESLint、Web build、`git diff --check` 通过。

### 2026-08-17 百艺全行为执行扩展

- 百艺子菜单扩展为采药、挖矿、炼丹、炼器四个真实可执行入口：`/craft/herbalism`、`/craft/mining`、`/craft/alchemy`、`/craft/forging`；全部作为 `craft.children` 紧跟百艺主菜单，技能等级区块不再承载行为入口。
- 新增通用 `BehaviorPage` 与行为适配器：采药 / 挖矿按六个地图区域展示，空区域仍可选择并显示“暂无可执行行动”；炼丹按基础、突破、战斗 / 恢复分类，炼器按工具、武器、防具分类。
- 炼丹 / 炼器读取现有 `getRecipes()` 配方接口，输入材料、产物、解锁和队列状态以 `RecipeCatalogEntry` 为权威；行动目录只负责关联队列 action 和验证 `INFINITE` 模式，不用行动输出冒充配方数据。
- 四类行为复用同一个队列触发 helper：单行动无限挂机、409 队列版本重试、暂停队列恢复、统一反馈，以及 queue / actions / recipes / progression / dashboard / 顶部进度查询失效。
- 配置里当前没有独立淬炼挂机行动，因此未伪造淬炼子页面；装备淬炼继续保留在角色装备流程。
- 验证：聚焦测试 `3 files / 19 tests passed`；Web 全量测试 `32 files / 139 tests passed`；Web typecheck、全仓 ESLint、Web build、`git diff --check` 通过。Node `22.22.3` 相对仓库要求 `24.x` 仍有 engine warning，构建保留既有 bundle size warning。

### 2026-08-17 洞府 / 修炼 / 历练职责重构

- 洞府主入口和根路径现在进入 `/dashboard/cave`，原挂机队列工作台移动到 `/dashboard/queue`；全局行动、内容来源和历练回流链接同步更新。洞府设施玩家文案改为“练功房 / 炼丹炉 / 锻造炉”，后端设施 ID 和建造升级接口保持不变。
- 新增 `/cultivation` 修炼方向选择页和 `/cultivation/breakthrough` 突破子页。练气复用真实 `action.cultivation.qi` 行动启动无限挂机；练体、练剑、练刀因为当前发布配置没有对应行动，保留入口但明确不可执行，不伪造速率。
- 历练页改为六个区域 → 区域怪物 → 掉落与战力信息；静态目录覆盖发布配置中的 11 只怪物，青蛇洞、玄铁洞窟、黑风秘境三套秘境根据所选怪物动态预览、进入和恢复运行，未配置秘境的怪物保留为“暂未开放”。
- 验证：Web 全量测试 `34 files / 141 tests passed`；Web typecheck、全仓 ESLint、Web build、`git diff --check` 通过。Node `22.22.3` 相对仓库要求 `24.x` 的 engine warning 和既有 bundle size warning 仍存在。

### 2026-08-17 武器专精修炼

- 修炼页新增武器专精分区：剑术、刀术、枪术、杖术四类专精均有真实技能配置和 60 秒无限修炼行动，技能 XP / 等级 / 当前攻击加成在页面展示，并复用现有队列冲突重试、暂停恢复和顶部进度刷新。
- 技能配置新增 `attack_bonus_per_level = 0.02`，并补齐 XP 曲线、功能解锁、内容中文标签和发布 manifest hash；修复过一次因缺少 `feature.weapon_mastery.*` 解锁条目导致内容 API 报 `CONFIG_NOT_FOUND` 的问题。
- 历练战斗构建玩家快照时读取已装备武器的 item tags，匹配对应武器专精等级后只放大武器攻击；零级、未匹配武器、防具和饰品均保持原逻辑。当前发布装备只有剑类武器，因此剑术专精可以立即影响战力，其他专精等待对应武器配置加入后自动生效。
- 验证：配置校验通过；全仓测试 `98 files / 410 tests passed`；全仓 typecheck、lint、API/Web build、`git diff --check` 通过。Node `22.22.3` 相对仓库要求 `24.x` 仍有 engine warning，Vite 保留既有大 chunk warning。
- 2026-08-18 运行态修复：修炼页出现“修炼内容暂时无法读取”，服务端日志确认 `GET /api/v1/actions` 因旧进程未重新加载 `feature_unlocks.json` 而返回 `CONFIG_NOT_FOUND:feature.weapon_mastery.sword`；队列与进度接口均为 200。API 的 `tsx watch` 只监听源码，不监听发布配置目录，因此补齐 feature unlock 后仍需重启 API。已重启本地 API 并重新加载完整专精配置；配置校验和全仓测试此前均通过。
- 运行态后续处理：原 PostgreSQL 服务不可用且 Docker CLI 不在本机，已启动 Homebrew PostgreSQL 18，创建本地 `dongtian` 开发角色/数据库并执行 13 条迁移；API readiness 恢复 200，匿名会话后的 `/api/v1/actions` 返回 200、39 个行动。该本地库是新初始化实例，旧数据库中的匿名角色数据需要原数据库或备份才能恢复。

### 2026-08-18 全页面单屏布局收口

- 将主内容区固定在 `960×600` 游戏视口内，`.shell-main__content` 不再产生页面级滚动；修炼、历练、百艺、洞府、挂机队列、炼丹、炼器、采药、挖矿和突破页统一使用密集网格。
- 修炼页将修炼方向与武器专精并列展示，四类武器专精卡片改为两列紧凑布局；历练页将地图、怪物、准备、运行和补充入口压入同一屏，并修正准备表单错误选择器导致的输入框溢出。
- 百艺总览的 39 个技能改为 7 列、35px 紧凑卡片，保留开始执行入口和耗时摘要，详细内容继续由详情弹窗承载；洞府详情操作、挂机队列结算摘要同步压缩，避免底部日志遮挡按钮。
- 增加折叠挂机设置的明确 `display:none` 规则，避免 `<details>` 关闭时隐藏内容仍参与布局并把主任务区压扁。
- 浏览器验证：`960×600` 扫描 10 个核心路由，`document` 高度均为 600，主内容 `scrollHeight === clientHeight === 350`，无横向溢出和可见内容越界；抽查百艺四个子页面、修炼、历练、洞府和当前任务截图无文字重叠或关键按钮裁切。
- 说明：`760px` 窄视口仍按移动端堆叠规则运行，部分高密度页面会进入内容压缩态；本轮验收基准为项目定义的 `960×600` 智能屏视口。
- 计划与规范已同步更新：`docs/superpowers/specs/2026-08-18-single-viewport-layout-design.md`、`docs/superpowers/plans/2026-08-18-single-viewport-layout.md`。

### 2026-08-18 修炼方向与武器修炼层级合并

- 修正修炼页的信息架构：移除独立的“修炼武器专精”面板，将 `练气、练体、练剑、练刀、练枪、练杖` 合并为同一个“修炼方向”目录，六项处于同等层级。
- `练剑、练刀、练枪、练杖` 继续复用原有武器专精行动、技能等级、经验与装备攻击加成数据；专精成为武器修炼项的成长属性，不再作为第二套修炼入口。
- 同步统一玩家可见名称：内容目录和挂机队列中的武器专精行动统一显示为 `练剑、练刀、练枪、练杖`，避免执行后又出现“修炼剑术专精”等重复称谓。
- 页面改为 3 列 2 行紧凑卡片，保留可执行状态、等级、攻击加成、经验和开始按钮；`960×600` 实测页面与主内容区无纵向/横向溢出，且不存在旧专精面板。
- 验证通过：全量 98 个测试文件、411 个测试，workspace typecheck、lint、Web build 和 `git diff --check` 均通过；Node 22 相对项目声明的 Node 24 仍仅产生已有环境警告。

### 2026-08-18 核心功能、游玩主链与银河奶牛对齐审查

- 审查结论：工程纵向切片主体存在，但核心功能不能判定为开发完成，真实新角色成长主链当前不通；银河奶牛只对齐了时间结算、行动队列和部分页面骨架，没有对齐完整玩法与经济闭环。
- P0 境界状态断裂：结算只增加 `character_progression.cultivation_xp`；角色进度接口按 XP 临时推导境界，但内容目录和队列权限读取持久化的 `realm_stage_id`。除筑基完成外没有代码同步炼气前、中、后、大圆满阶段，导致修为达到阈值后页面显示新境界，采药、炼丹、挖矿、秘境和洞府仍按凡人境锁定。
- 浏览器真实复现：同一角色显示 `2371.5` 修为和“炼气中期”，百艺页的采药、炼丹、挖矿等仍全部锁定；只有初始修炼与四类武器修炼可执行。现有 E2E 夹具会直接写 `realm_stage_id`，因此此前通过记录没有覆盖自然成长解锁。
- P0 教程门槛永久锁定：内容权限没有读取角色教程进度，而是以 `required_tutorial_ids.length === 0` 判断教程完成。凡配置了必需教程的功能都会永久不可用；当前采药与炼丹分别受 `TUT-001`、`TUT-002` 阻断。这是本次纵向 E2E 在百艺页看不到可用采药行动的直接原因。
- P1 浏览器回归失效：本次外部 PostgreSQL 18 + Chromium 执行 5 条 Playwright 规格，筑基 3 条通过，纵向切片在百艺页失败，随后 1 条串行用例未运行。CI 只执行 `test:e2e:smoke`（测试发现），不会运行真实 E2E，因此教程永久锁定与页面断言过期均无法阻断合入。
- P1 时间反馈漂移：配置中灵气修炼为 60 秒，但全局进度和任务页仍使用硬编码 100 秒；浏览器实测同一行动在修炼卡显示“每轮 1 分钟”，顶栏显示“本轮还需 1 分 40 秒”。采药也存在配置 100 秒、进度映射 140 秒的同类问题。
- P1 首次游玩入口不顺：根路由进入全部锁定的洞府设施页，曾作为首屏的快捷任务 / 高级队列页位于 `/dashboard/queue`，但不在主导航；指南仍写“进入洞府后选择任务”，与当前页面不符。玩家只能自行发现“修炼”入口。
- P1 一键启动环境不完整：`启动洞天.command` 只启动 API 和 Web，没有启动承担 Outbox、长结算续算、洞府及筑基恢复的 Worker；本地可体验环境与 README 所述服务端能力不完全一致。
- 银河奶牛差距：官方页面明确将多人放置、采集制造、自动战斗、玩家市场、公会聊天和排行榜列为核心；当前市场、商店、牛铃商店、宗门、社交、成就和排行榜均为锁定页，“任务”只是行动队列的另一种展示，不是带任务槽、奖励与任务币的任务系统。当前可称为银河奶牛式单人挂机骨架，不能称为玩法对齐。
- 内容与发布缺口：release 资产校验仍被 22 个占位资源阻断，正式音频、美术和多个长期系统未完成；版本仍不满足 Alpha / 发布标准。
- 本次验证：`pnpm test` 通过（98 文件 / 411 测试），`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm config:validate` 通过；`pnpm asset:validate` 失败（22 个 release blocker）；真实 Playwright 为 3 passed / 1 failed / 1 did not run。Node 仍为 22.22.3，低于项目声明的 24.x。

### 2026-08-18 单人挂机主链整改与历练简化

- 按银河奶牛式单击挂机逻辑收口单人入口：修炼、采药、挖矿、炼丹、炼器继续使用目标卡片直接开始；历练改为“选择地图 → 选择怪物 → 开始战斗挂机”，默认路由不再进入策略、路线和秘境确认流程。
- 发布配置新增 11 个无限战斗行动，覆盖青云山麓到黑风谷的 11 只怪物，并补齐耗时、修为和掉落；发布配置 hash 更新为 `sha256:c58f187e9196c38fd8d25404dd7d7aedd6fab62b164c75d6beb963765e65b408`。
- 修复自然成长权限断链：内容、队列、洞府、秘境、装备和角色接口统一按修为计算有效炼气境界；炼气阶段可自然推进，筑基仍必须完成突破，不会被修为自动跳过。
- 教程 ID 不再充当永久功能锁，采药、炼丹等真实行动可按境界和配置正常解锁；全局进度与任务摘要改为读取服务端 `base_duration_us`，消除卡片与顶栏耗时不一致。
- 单击挂机请求新增 `replace_current` 语义：事务先结算旧行动到点击时刻，再取消旧行动、启动新行动并同步结算快照。修复了“页面提示已开始但队列 current 为空或仍是旧行动”的问题，青蛇点击后接口当前行动立即为 `action.t1.combat_qingshe`。
- 修复极短结算片段产生 `0` 数量物品奖励并触发 `ASSET_VALIDATION_FAILED:item_quantity` 的问题；零数量奖励不再写入资产账本。
- 装备预设 409 冲突现在通过全局操作反馈直接显示，固定单屏布局下无需滚动即可看到错误，未保存草稿仍会保留。
- 主导航移除未完成的商店、牛铃商店、任务、迷宫、新闻、更新日志和成就占位入口；修炼页移除没有真实行动的练体卡。旧路由继续保留，联网相关的市场、宗门、仙友和修行榜仍明确锁定，未开发联网逻辑。
- 根路由进入修炼页；本地启动脚本同时启动 API、Worker、Web；CI 从 E2E 冒烟发现改为执行真实 Playwright 套件。
- 最终验证：`pnpm test` 通过（98 文件 / 417 测试），`pnpm typecheck`、`pnpm build`、`pnpm config:validate`、`pnpm openapi:check`、`git diff --check` 通过；真实 Playwright `5/5` 通过。Node 22.22.3 仍低于项目声明的 Node 24.x，Web 构建仍有既有大 chunk 警告。
- 未完成边界：成就 / 收藏 / 妖兽图鉴仍未开发，只是从主导航移除；正式发布资产仍有此前记录的 22 个占位资源阻断。当前可判定为可连续游玩的单人挂机主链，不应表述为正式发布完成。

### 2026-08-18 单人目标卡单击启动与 Worker 运行态收口

- 进一步按银河奶牛的直接操作收口：历练页现在是“选择地图 → 点击怪物卡”，点击可用怪物立即替换当前挂机并开始战斗；采药、挖矿、炼丹、炼器和修炼的可执行目标卡也统一改为单击启动，移除二次“开始”按钮。锁定目标仍只展示解锁原因，不会触发队列写入。
- 目标卡增加键盘 Enter / Space 启动、启动中状态和可见反馈；战斗 E2E 改为点击“青蛇，点击开始战斗挂机”目标按钮，服务端继续验证当前行动立即切换为 `action.t1.combat_qingshe`。
- 修复 Worker 通过 `tsx src/main.ts` 启动时后四个 Nest 构造依赖缺少显式 `@Inject` 导致 `runOnce` 为 `undefined` 的运行时故障；补充构造依赖元数据回归测试，真实 Worker 进程已能持续启动且无该错误。
- 最终验证：Vitest `98 files / 418 tests passed`；Worker 定向测试 `5/5`；`pnpm typecheck`、`pnpm build`、`pnpm config:validate`、`pnpm openapi:check`、`git diff --check` 全部通过；真实 Playwright `5/5` 通过。
- 当前边界不变：联网玩法（市场交易、宗门、仙友、修行榜）仍锁定；成就 / 收藏 / 妖兽图鉴未开发且已移出主导航；发布资产仍有此前记录的 22 个占位资源阻断。Node `22.22.3` 低于项目声明的 `24.x`，Web 仍有既有大 chunk 警告。

### 2026-08-19 GitHub 同步

- 已将单人挂机主链整改提交 `fa0152b` 推送至 GitHub `origin/main`。

### 2026-08-19 AI 生图 UI 测试提示词

- 根据当前游戏定位整理 UI 生图测试提示词：以 960×600 智能屏视口、左侧主导航、修炼总览和“地图 → 怪物 → 单击挂机”的历练主链为基准，强调简单直接的单人放置玩法，避免复杂策略面板和 MMO 式信息堆叠。
- 已扩展为统一页面体系：新增统一母提示词、负面提示词和洞府、修炼、突破、百艺总览、采药、挖矿、炼丹、炼器、历练、角色装备、背包、设置、指南、规则共 14 个页面提示词；联网玩法按当前范围不生成。
